import HeroCarousel from "./navbar/hero/page";

const page = () => {
  return (
    <main className="min-h-screen flex flex-col">
      <HeroCarousel />
      {/* other sections */}
    </main>
  );
};

export default page;
